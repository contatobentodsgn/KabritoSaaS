Content surface — 12px corners, hairline border on white. The system workhorse.

```jsx
<Card variant="default">
  <h3 className="k-heading-3">Card padrão</h3>
  <p className="k-body-sm">Mint Cream como base, textos em verde e um toque de rosa.</p>
</Card>
<Card variant="elevated" interactive>…</Card>
<Card variant="highlight">…</Card>      {/* blush wash — emotional blocks */}
<Card variant="editorial">…</Card>      {/* forest fill, light text — hero moment */}
```

Variants: `default` · `elevated` (soft shadow) · `highlight` (Powder Blush) · `editorial` (Black Forest fill). Reserve `editorial` for a single hero moment per page.
