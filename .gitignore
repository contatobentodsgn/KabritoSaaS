# dependencies
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files — NUNCA versionar segredos (SECURITY_GUIDE §5)
.env
.env*.local
.env.local

# local postgres data (test harness)
/.pgdata/
/.pglogs/

# vercel
.vercel

# typescript
*.tsbuildinfo

# test
/coverage

# binários/artefatos que não devem ir ao repo
/dist
*.dmg
*.zip

# backups do banco — contêm DADOS DE USUÁRIO; o repo é PÚBLICO, então NUNCA
# versionar. O db:backup grava em ~/kabrito-backups (fora do repo); estes
# padrões são defesa extra caso algum dump caia aqui.
/backups/
*.sql.gz
kabrito-*.sql
